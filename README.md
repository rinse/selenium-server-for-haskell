# a chrome webdriver with [WebDriver](https://hackage.haskell.org/package/webdriver-0.9.0.1)

1. go `$ docker-compose up`
  * it opens 4444 the default port of the standalone selenium server (Grid)

2. boom

[Sample.hs](Sample.hs)
